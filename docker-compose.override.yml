# development configuration
version: '3.9'
services:

  stocks-service:
    build:
      context: .
      dockerfile: ./stocks-service/Dockerfile
  consumer:
    build:
      context: .
      dockerfile: ./consumer/Dockerfile